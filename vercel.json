{
  "version": 2,
  "builds": [
    { "source": "source/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/source/$1" }
  ]
}
