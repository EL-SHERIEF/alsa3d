{
  "version": 2,
  "builds": [
    { "src": "source/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/source/$1" }
  ]
}
